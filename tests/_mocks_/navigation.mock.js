// Mock the navigation props
export const navigation = {
    navigate: jest.fn(),
    goBack: jest.fn(),
    dispatch: jest.fn(),
    addListener: jest.fn(),
};

export const route = {
    params: {},
};
